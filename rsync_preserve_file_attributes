rsync -avz /source/directory /destination/directory

or 

rsync -avz /local/directory username@remote-server:/remote/directory

To Avoid Compression and want know anout the Progress of the file movement

rsync -avP /local/directory /destination/directory

or 

rsync -avP /local/directory username@remote-server:/remote/directory

In this command:

-a stands for "archive mode", which preserves file attributes like ownership, permissions, timestamps, etc.
-v stands for "verbose", which provides detailed information about the operations being performed.
-z stands for "compress", which compresses data during the transfer 

